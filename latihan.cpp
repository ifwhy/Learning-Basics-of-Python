#include <iostream>
using namespace std;

int main()
{
    int n, sum = 0;
    

    // Untuk keluar tekan shift + alt + M
    while(cin >> n){
        sum += n;
    }
    cout << sum << endl;
    return 0;
}
